extends Node2D


# Declare member variables here. Examples:
# var a = 2
# var b = "text"
var a :TileMap = null


# Called when the node enters the scene tree for the first time.
func _ready():
	#print("ready!!!!")
	#a = preload("res://World/TileMap.gd").new()
	#print(a.get_used_cells(),"aaaaa")
	#var a = preload("res://World/TileMap.gd")
	#a.a()
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
